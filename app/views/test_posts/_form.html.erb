<%= form_with(model: test_post) do |form| %>
  <% if test_post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(test_post.errors.count, "error") %> prohibited this Post from being saved:</h2>

      <ul>
        <% test_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>
  
  <div>
    <%= form.submit %>
  </div>
<% end %>
